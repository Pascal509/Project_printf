#include "main.h"

/**
 * is_Digit - Checks for a digit
 * @n: The character to checke
 * Return: 1 if n is digit, else 0
 */
int is_Digit(int n)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
}
